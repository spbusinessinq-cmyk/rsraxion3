AXION production is STILL only pulling ~95 raw signals live.

This means the live deployed site is still not successfully using the Node proxy route /api/proxy/rss, even though preview/dev gets 500 usable signals.

Perform a full production deployment diagnostic and hard fix.

DO NOT redesign UI.
DO NOT change report design.
DO NOT change styling.
ONLY fix production signal count.

GOAL:
Production live site must show:
RAW COLLECTED: ~900+
USABLE SIGNALS: 500
FALLBACK: 0
FEEDS_OK: ~50+/78

━━━━━━━━━━━━━━━━━━━━━━
1. VERIFY NODE SERVER CONFIG
━━━━━━━━━━━━━━━━━━━━━━

Confirm package.json has:

"scripts": {
  "start": "node server.mjs",
  "build": "vite build",
  "dev": "vite --host 0.0.0.0"
}

If missing, fix it.

Confirm server.mjs:
- imports express
- serves dist
- exposes GET /api/proxy/rss
- listens on process.env.PORT || 5000
- returns frontend for non-api routes
- logs when /api/proxy/rss is hit

Add this log inside proxy route:
console.log("[AXION PROXY HIT]", url)

━━━━━━━━━━━━━━━━━━━━━━
2. ADD LIVE PROXY HEALTH CHECK
━━━━━━━━━━━━━━━━━━━━━━

Add endpoint:

GET /api/health

Return JSON:

{
  status: "AXION NODE ONLINE",
  proxy: true,
  buildVersion: "AXION-v3-signal-500-live",
  timestamp: new Date().toISOString()
}

This lets us verify whether production is actually running Node.

━━━━━━━━━━━━━━━━━━━━━━
3. FRONTEND PROXY DETECTION
━━━━━━━━━━━━━━━━━━━━━━

Before pulling signals, frontend must test:

fetch("/api/health")

If success:
- set PROXY_MODE = "node"
- use /api/proxy/rss as primary
- show status/debug: PROXY: NODE

If fail:
- set PROXY_MODE = "static"
- use rss2json/allorigins fallback
- show warning/debug: PROXY: STATIC FALLBACK

Add this to console:

[AXION PRODUCTION MODE]
BUILD_VERSION:
PROXY_MODE:
HEALTH_OK:
API_PROXY_AVAILABLE:

━━━━━━━━━━━━━━━━━━━━━━
4. BLOCK FALSE SUCCESS
━━━━━━━━━━━━━━━━━━━━━━

Do NOT silently accept 95 signals as success.

If PROXY_MODE is static and usable signals < 300:
Show internal status chip:
"STATIC FALLBACK — NODE PROXY OFFLINE"

Do not mark this as full live success.

━━━━━━━━━━━━━━━━━━━━━━
5. FIX FETCH STRATEGY
━━━━━━━━━━━━━━━━━━━━━━

When PROXY_MODE = node:
Use only /api/proxy/rss first.

Do not send 78 feeds directly to allorigins unless Node proxy is unavailable.

When Node proxy is available:
- batch feeds safely
- collect up to 25 items/feed
- keep top 500 usable signals
- fallback count should be 0 unless feed data is truly insufficient

━━━━━━━━━━━━━━━━━━━━━━
6. EDGEONE DEPLOYMENT COMPATIBILITY
━━━━━━━━━━━━━━━━━━━━━━

Make sure this app can run as Node/Autoscale.

Add or confirm:
- server.mjs exists inside deployed repo root
- package.json start script exists
- dist is generated by npm run build
- server.mjs serves dist/index.html
- server.mjs handles /api/proxy/rss before static fallback

Document exact EdgeOne settings needed in final response:

Root directory:
axion-repo

Install command:
npm install

Build command:
npm run build

Start command:
npm start

Runtime:
Node / Autoscale

Entry:
server.mjs

━━━━━━━━━━━━━━━━━━━━━━
7. BUILD VERSION CACHE BUST
━━━━━━━━━━━━━━━━━━━━━━

Set:

BUILD_VERSION = "AXION-v3-node-proxy-required"

If build version changes:
- clear live signal cache/session signal state
- keep brief archive
- keep analyst notes if safe

━━━━━━━━━━━━━━━━━━━━━━
8. FINAL TEST
━━━━━━━━━━━━━━━━━━━━━━

Run build.

Then test locally/preview:
- /api/health returns AXION NODE ONLINE
- /api/proxy/rss logs [AXION PROXY HIT]
- Pull Signals produces:
  RAW_COLLECTED ~900+
  USABLE 500
  FALLBACK 0

Final response must include:
- package.json start script confirmed
- server.mjs proxy route confirmed
- /api/health confirmed
- PROXY_MODE result
- raw collected
- usable signals
- fallback count
- build result

IMPORTANT:
If production still shows 95 after this, the code is not the issue. The EdgeOne project is deployed as STATIC instead of NODE/AUTOSCALE or is pointing at the wrong root/build.